import { FormControl, styled } from '@mui/material';

export const StyledFromControl = styled(FormControl)(({ theme }) => ({
    margin: theme.spacing(1),
    minWidth: 120,
    marginBottom: '30px',
}));
